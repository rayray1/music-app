import { ReactNode } from "react";
import clsx from "clsx"

type ButtonProps = {
  className: string;
  children: ReactNode;
  onClick: React.MouseEventHandler<HTMLButtonElement>

  export function Button({params}:type) {
    
  }
}